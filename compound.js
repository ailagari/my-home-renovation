export const compoundDefaults={visible:true,height:2,left:1,right:1,front:5,gateWidth:3.6,gateSide:'left',color:'#d9d7c7'};
export function compoundSettings(value={}){const c={...compoundDefaults,...value};for(const [key,min,max] of [['height',.5,3],['left',.2,5],['right',.2,5],['front',1,15],['gateWidth',1,8]])if(!Number.isFinite(c[key])||c[key]<min||c[key]>max)throw Error('Compound dimensions are outside the supported range.');if(typeof c.visible!=='boolean'||!['left','right'].includes(c.gateSide)||!/^#[a-f0-9]{6}$/i.test(c.color)||c.gateWidth>8.6+c.left+c.right-.5)throw Error('Invalid compound wall or gate settings.');return c;}
export function compoundQuantities(value){const c=compoundSettings(value),length=2*(12.85+c.front)+8.6+c.left+c.right-c.gateWidth;return {length,faceArea:length*c.height,finishArea:length*c.height*2,gateArea:c.gateWidth*Math.min(c.height,1.9)};}
